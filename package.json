{
  "name": "vitepress-helper",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docs:dev": "turbo run docs:dev --no-daemon",
    "docs:dev:root": "turbo run docs:dev:root --no-daemon",
    "docs:dev:src": "turbo run docs:dev:src --no-daemon",
    "build": "turbo run build --no-daemon",
    "dev": "turbo run dev --no-daemon"
  },
  "keywords": [],
  "packageManager": "pnpm@10.12.2",
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "turbo": "^2.5.3",
    "vitepress": "1.6.3"
  }
}
